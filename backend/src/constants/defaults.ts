/**
 * Default constants for the application
 */

export const DEFAULT_ARTICLE_COUNT = 10;
export const LLM_CONCURRENCY_LIMIT = 3;
export const LLM_TIMEOUT_MS = 30000;
export const LLM_MAX_RETRIES = 1;
export const RSS_FETCH_TIMEOUT_MS = 10000;
